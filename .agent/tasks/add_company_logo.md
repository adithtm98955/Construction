# Task: Add Company Logo to All Pages

The user requested to add `xsd.jpg` to all navbars and footers as the company logo.

## Changes Applied

The following HTML files were updated:
- `index.html`
- `about.html`
- `contact.html`
- `services.html`
- `details.html`

### Updates in each file:
1.  **Navbar**: Replaced the text logo (e.g., `<span class="text-warning">CON</span>STRUCT`) with `<img src="xsd.jpg" alt="Construct Logo" style="height: 40px;">`.
2.  **Footer**: Replaced the text logo with the same image tag.
3.  **Mobile Menu (Modal)**: Updated the modal title to display the logo image instead of text or the old placeholder image.

## Verification
- Checked all main HTML pages to ensure consistency.
- Used `style="height: 40px;"` to ensure the logo is appropriately sized within the navigation bars.
